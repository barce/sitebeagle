#!/usr/bin/env newlisp

;
; class Twitter
; -------------
;
; used for messaging users on twitter
(context 'Twitter)
(set 'user nil)
(set 'pass nil)
(set 'recipient nil)

; dm : send a direct message
(define (dm message))


(context MAIN)

